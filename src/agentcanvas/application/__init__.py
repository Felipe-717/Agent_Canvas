"""Capa de aplicacion: casos de uso y puertos.

Orquesta el dominio a traves de interfaces (`ports`). Igual que `domain`, no
importa nada de `infrastructure`: son los adaptadores los que dependen de los
puertos, nunca al reves.
"""
